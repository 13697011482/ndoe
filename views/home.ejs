<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <%- include('template/file') %>
  <link rel="stylesheet" href="stylesheets/lagou.css">
</head>
<body>
  
    <% workList.forEach((v,i) => { %>
      <div id="work_content" class="border border-success p-2 mb-2">
      <a class="work_item d-block text-decoration-none" href="/detail/<%= v._id %>">
        <div class="top">
          <p class="d-inline-block"><%=v.name %></p>
          <span class="ml-2"><%=v.location %></span>
          <span class="float-right"><%=v.company %></span>
        </div>
          <span class="text-danger mr-5"><%=v.wages %></span>
          <span class="mr-5"><%=v.education %></span>
          <span><%=v.number %></span>
        </div>
      </a>
    </div>
    <% }) %>
  </div>
</body>
</html>